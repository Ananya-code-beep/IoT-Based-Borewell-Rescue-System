TO push it:
unzip borewell-rescue-system-github.zip
cd repo
git init
git add .
git commit -m "Initial commit: Borewell Rescue System using IoT"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
